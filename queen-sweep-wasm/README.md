# queen-sweep-wasm

WASM layer for the core solver engine. Compiles the Rust logic to WebAssembly through `wasm-bindgen`, enabling in-browser puzzle solving

## Running
```bash
npm run build:wasm
npm run test
````

## Usage

See the example in [`index.ts`](./index.ts)