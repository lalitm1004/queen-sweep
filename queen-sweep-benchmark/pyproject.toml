[project]
name = "process-benchmark"
version = "0.1.0"
description = "A simple script to process and visualize the benchmark.csv files generated by the main binary"
readme = "README.md"
requires-python = ">=3.14"
dependencies = ["matplotlib>=3.10.8", "pandas>=2.3.3"]
